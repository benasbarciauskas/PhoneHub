import SwiftUI

@main
struct PhoneHubApp: App {
    var body: some Scene {
        WindowGroup("PhoneHub") {
            Text("PhoneHub")
                .frame(minWidth: 900, minHeight: 600)
        }
    }
}
